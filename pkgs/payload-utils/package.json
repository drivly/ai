{
  "name": "@ai-primitives/payload-utils",
  "version": "0.1.0",
  "type": "module",
  "description": "Payload CMS utilities for Cloudflare Workers and NextJS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "lint": "eslint src/**/*.ts",
    "test": "echo \"No tests\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "payload",
    "cms",
    "cloudflare",
    "workers",
    "nextjs"
  ],
  "author": "Drivly",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.11",
    "tsup": "^7.2.0",
    "typescript": "^5.8.2"
  }
}
