/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsDeleteApiEventsId } from "../../funcs/eventsDeleteApiEventsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiEventsIdRequest$inboundSchema,
};

export const tool$eventsDeleteApiEventsId: ToolDefinition<typeof args> = {
  name: "events-delete-api-events-id",
  description: `Delete a Event

Deletes a specific Event by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsDeleteApiEventsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
