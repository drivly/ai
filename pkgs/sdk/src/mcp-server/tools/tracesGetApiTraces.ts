/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tracesGetApiTraces } from "../../funcs/tracesGetApiTraces.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$tracesGetApiTraces: ToolDefinition = {
  name: "traces-get-api-traces",
  description: `List all Traces

Returns a list of all Traces`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await tracesGetApiTraces(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
