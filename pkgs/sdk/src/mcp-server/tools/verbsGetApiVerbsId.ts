/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verbsGetApiVerbsId } from "../../funcs/verbsGetApiVerbsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiVerbsIdRequest$inboundSchema,
};

export const tool$verbsGetApiVerbsId: ToolDefinition<typeof args> = {
  name: "verbs-get-api-verbs-id",
  description: `Get a specific Verb

Returns a specific Verb by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verbsGetApiVerbsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
