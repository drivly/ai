/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { goalsDeleteApiGoalsId } from "../../funcs/goalsDeleteApiGoalsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiGoalsIdRequest$inboundSchema,
};

export const tool$goalsDeleteApiGoalsId: ToolDefinition<typeof args> = {
  name: "goals-delete-api-goals-id",
  description: `Delete a Goal

Deletes a specific Goal by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await goalsDeleteApiGoalsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
