/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowsGetApiWorkflowsId } from "../../funcs/workflowsGetApiWorkflowsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiWorkflowsIdRequest$inboundSchema,
};

export const tool$workflowsGetApiWorkflowsId: ToolDefinition<typeof args> = {
  name: "workflows-get-api-workflows-id",
  description: `Get a specific Workflow

Returns a specific Workflow by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsGetApiWorkflowsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
