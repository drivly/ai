/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { thingsGetApiThings } from "../../funcs/thingsGetApiThings.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$thingsGetApiThings: ToolDefinition = {
  name: "things-get-api-things",
  description: `List all Things

Returns a list of all Things`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await thingsGetApiThings(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
