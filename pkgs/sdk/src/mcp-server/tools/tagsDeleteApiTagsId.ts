/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tagsDeleteApiTagsId } from "../../funcs/tagsDeleteApiTagsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiTagsIdRequest$inboundSchema,
};

export const tool$tagsDeleteApiTagsId: ToolDefinition<typeof args> = {
  name: "tags-delete-api-tags-id",
  description: `Delete a Tag

Deletes a specific Tag by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tagsDeleteApiTagsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
