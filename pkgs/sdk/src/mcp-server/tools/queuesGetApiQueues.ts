/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queuesGetApiQueues } from "../../funcs/queuesGetApiQueues.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$queuesGetApiQueues: ToolDefinition = {
  name: "queues-get-api-queues",
  description: `List all Queues

Returns a list of all Queues`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await queuesGetApiQueues(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
