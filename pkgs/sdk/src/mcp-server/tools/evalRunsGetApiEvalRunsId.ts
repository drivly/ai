/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalRunsGetApiEvalRunsId } from "../../funcs/evalRunsGetApiEvalRunsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiEvalRunsIdRequest$inboundSchema,
};

export const tool$evalRunsGetApiEvalRunsId: ToolDefinition<typeof args> = {
  name: "eval-runs-get-api-eval-runs-id",
  description: `Get a specific Eval Run

Returns a specific Eval Run by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalRunsGetApiEvalRunsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
