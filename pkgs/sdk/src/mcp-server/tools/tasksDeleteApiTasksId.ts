/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksDeleteApiTasksId } from "../../funcs/tasksDeleteApiTasksId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiTasksIdRequest$inboundSchema,
};

export const tool$tasksDeleteApiTasksId: ToolDefinition<typeof args> = {
  name: "tasks-delete-api-tasks-id",
  description: `Delete a Task

Deletes a specific Task by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksDeleteApiTasksId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
