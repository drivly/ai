/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tracesDeleteApiTracesId } from "../../funcs/tracesDeleteApiTracesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiTracesIdRequest$inboundSchema,
};

export const tool$tracesDeleteApiTracesId: ToolDefinition<typeof args> = {
  name: "traces-delete-api-traces-id",
  description: `Delete a Trace

Deletes a specific Trace by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tracesDeleteApiTracesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
