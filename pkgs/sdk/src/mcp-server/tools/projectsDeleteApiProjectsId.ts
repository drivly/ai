/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsDeleteApiProjectsId } from "../../funcs/projectsDeleteApiProjectsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiProjectsIdRequest$inboundSchema,
};

export const tool$projectsDeleteApiProjectsId: ToolDefinition<typeof args> = {
  name: "projects-delete-api-projects-id",
  description: `Delete a Project

Deletes a specific Project by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsDeleteApiProjectsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
