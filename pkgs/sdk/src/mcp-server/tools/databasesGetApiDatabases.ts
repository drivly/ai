/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databasesGetApiDatabases } from "../../funcs/databasesGetApiDatabases.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$databasesGetApiDatabases: ToolDefinition = {
  name: "databases-get-api-databases",
  description: `List all Databases

Returns a list of all Databases`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await databasesGetApiDatabases(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
