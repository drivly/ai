/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { triggersDeleteApiTriggersId } from "../../funcs/triggersDeleteApiTriggersId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiTriggersIdRequest$inboundSchema,
};

export const tool$triggersDeleteApiTriggersId: ToolDefinition<typeof args> = {
  name: "triggers-delete-api-triggers-id",
  description: `Delete a Trigger

Deletes a specific Trigger by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await triggersDeleteApiTriggersId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
