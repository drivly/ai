/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queuesDeleteApiQueuesId } from "../../funcs/queuesDeleteApiQueuesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiQueuesIdRequest$inboundSchema,
};

export const tool$queuesDeleteApiQueuesId: ToolDefinition<typeof args> = {
  name: "queues-delete-api-queues-id",
  description: `Delete a Queue

Deletes a specific Queue by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await queuesDeleteApiQueuesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
