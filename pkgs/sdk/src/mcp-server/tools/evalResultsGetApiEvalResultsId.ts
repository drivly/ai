/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalResultsGetApiEvalResultsId } from "../../funcs/evalResultsGetApiEvalResultsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiEvalResultsIdRequest$inboundSchema,
};

export const tool$evalResultsGetApiEvalResultsId: ToolDefinition<typeof args> =
  {
    name: "eval-results-get-api-eval-results-id",
    description: `Get a specific Eval Result

Returns a specific Eval Result by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await evalResultsGetApiEvalResultsId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
