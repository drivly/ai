/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { actionsGetApiIntegrationActions } from "../../funcs/actionsGetApiIntegrationActions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$actionsGetApiIntegrationActions: ToolDefinition = {
  name: "actions-get-api-integration-actions",
  description: `List all Actions

Returns a list of all Actions`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await actionsGetApiIntegrationActions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
