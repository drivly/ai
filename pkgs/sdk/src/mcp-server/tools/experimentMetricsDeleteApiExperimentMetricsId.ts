/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentMetricsDeleteApiExperimentMetricsId } from "../../funcs/experimentMetricsDeleteApiExperimentMetricsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiExperimentMetricsIdRequest$inboundSchema,
};

export const tool$experimentMetricsDeleteApiExperimentMetricsId: ToolDefinition<
  typeof args
> = {
  name: "experiment-metrics-delete-api-experiment-metrics-id",
  description: `Delete a Experiment Metric

Deletes a specific Experiment Metric by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await experimentMetricsDeleteApiExperimentMetricsId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
