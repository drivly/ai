/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labsGetApiLabs } from "../../funcs/labsGetApiLabs.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$labsGetApiLabs: ToolDefinition = {
  name: "labs-get-api-labs",
  description: `List all Labs

Returns a list of all Labs`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await labsGetApiLabs(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
