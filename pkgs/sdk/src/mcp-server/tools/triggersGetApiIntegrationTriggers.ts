/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { triggersGetApiIntegrationTriggers } from "../../funcs/triggersGetApiIntegrationTriggers.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$triggersGetApiIntegrationTriggers: ToolDefinition = {
  name: "triggers-get-api-integration-triggers",
  description: `List all Triggers

Returns a list of all Triggers`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await triggersGetApiIntegrationTriggers(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
