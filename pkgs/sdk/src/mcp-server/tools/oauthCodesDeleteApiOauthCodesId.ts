/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthCodesDeleteApiOauthCodesId } from "../../funcs/oauthCodesDeleteApiOauthCodesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiOauthCodesIdRequest$inboundSchema,
};

export const tool$oauthCodesDeleteApiOauthCodesId: ToolDefinition<typeof args> =
  {
    name: "oauth-codes-delete-api-oauth-codes-id",
    description: `Delete a Oauth Code

Deletes a specific Oauth Code by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await oauthCodesDeleteApiOauthCodesId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
