/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentMetricsGetApiExperimentMetricsId } from "../../funcs/experimentMetricsGetApiExperimentMetricsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiExperimentMetricsIdRequest$inboundSchema,
};

export const tool$experimentMetricsGetApiExperimentMetricsId: ToolDefinition<
  typeof args
> = {
  name: "experiment-metrics-get-api-experiment-metrics-id",
  description: `Get a specific Experiment Metric

Returns a specific Experiment Metric by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentMetricsGetApiExperimentMetricsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
