/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthCodesGetApiOauthCodesId } from "../../funcs/oauthCodesGetApiOauthCodesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiOauthCodesIdRequest$inboundSchema,
};

export const tool$oauthCodesGetApiOauthCodesId: ToolDefinition<typeof args> = {
  name: "oauth-codes-get-api-oauth-codes-id",
  description: `Get a specific Oauth Code

Returns a specific Oauth Code by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauthCodesGetApiOauthCodesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
