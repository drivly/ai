/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetApiWebhooks } from "../../funcs/webhooksGetApiWebhooks.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$webhooksGetApiWebhooks: ToolDefinition = {
  name: "webhooks-get-api-webhooks",
  description: `List all Webhooks

Returns a list of all Webhooks`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await webhooksGetApiWebhooks(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
