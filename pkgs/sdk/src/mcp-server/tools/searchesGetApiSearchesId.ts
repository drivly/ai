/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchesGetApiSearchesId } from "../../funcs/searchesGetApiSearchesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiSearchesIdRequest$inboundSchema,
};

export const tool$searchesGetApiSearchesId: ToolDefinition<typeof args> = {
  name: "searches-get-api-searches-id",
  description: `Get a specific Search

Returns a specific Search by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchesGetApiSearchesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
