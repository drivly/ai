/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalResultsDeleteApiEvalResultsId } from "../../funcs/evalResultsDeleteApiEvalResultsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiEvalResultsIdRequest$inboundSchema,
};

export const tool$evalResultsDeleteApiEvalResultsId: ToolDefinition<
  typeof args
> = {
  name: "eval-results-delete-api-eval-results-id",
  description: `Delete a Eval Result

Deletes a specific Eval Result by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalResultsDeleteApiEvalResultsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
