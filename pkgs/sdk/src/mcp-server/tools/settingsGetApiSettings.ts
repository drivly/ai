/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { settingsGetApiSettings } from "../../funcs/settingsGetApiSettings.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$settingsGetApiSettings: ToolDefinition = {
  name: "settings-get-api-settings",
  description: `List all Settings

Returns a list of all Settings`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await settingsGetApiSettings(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
