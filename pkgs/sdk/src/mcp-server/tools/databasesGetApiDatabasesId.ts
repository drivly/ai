/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databasesGetApiDatabasesId } from "../../funcs/databasesGetApiDatabasesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiDatabasesIdRequest$inboundSchema,
};

export const tool$databasesGetApiDatabasesId: ToolDefinition<typeof args> = {
  name: "databases-get-api-databases-id",
  description: `Get a specific Database

Returns a specific Database by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await databasesGetApiDatabasesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
