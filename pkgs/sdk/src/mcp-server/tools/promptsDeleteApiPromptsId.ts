/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { promptsDeleteApiPromptsId } from "../../funcs/promptsDeleteApiPromptsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiPromptsIdRequest$inboundSchema,
};

export const tool$promptsDeleteApiPromptsId: ToolDefinition<typeof args> = {
  name: "prompts-delete-api-prompts-id",
  description: `Delete a Prompt

Deletes a specific Prompt by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await promptsDeleteApiPromptsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
