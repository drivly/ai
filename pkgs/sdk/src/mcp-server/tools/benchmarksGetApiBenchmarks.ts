/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { benchmarksGetApiBenchmarks } from "../../funcs/benchmarksGetApiBenchmarks.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$benchmarksGetApiBenchmarks: ToolDefinition = {
  name: "benchmarks-get-api-benchmarks",
  description: `List all Benchmarks

Returns a list of all Benchmarks`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await benchmarksGetApiBenchmarks(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
