/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsGetApiExperiments } from "../../funcs/experimentsGetApiExperiments.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$experimentsGetApiExperiments: ToolDefinition = {
  name: "experiments-get-api-experiments",
  description: `List all Experiments

Returns a list of all Experiments`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await experimentsGetApiExperiments(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
