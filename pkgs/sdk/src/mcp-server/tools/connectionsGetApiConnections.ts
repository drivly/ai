/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionsGetApiConnections } from "../../funcs/connectionsGetApiConnections.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$connectionsGetApiConnections: ToolDefinition = {
  name: "connections-get-api-connections",
  description: `List all Connections

Returns a list of all Connections`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await connectionsGetApiConnections(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
