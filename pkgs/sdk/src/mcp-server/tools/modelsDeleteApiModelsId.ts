/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { modelsDeleteApiModelsId } from "../../funcs/modelsDeleteApiModelsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiModelsIdRequest$inboundSchema,
};

export const tool$modelsDeleteApiModelsId: ToolDefinition<typeof args> = {
  name: "models-delete-api-models-id",
  description: `Delete a Model

Deletes a specific Model by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await modelsDeleteApiModelsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
