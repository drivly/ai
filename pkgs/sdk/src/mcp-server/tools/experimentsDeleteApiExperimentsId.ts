/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsDeleteApiExperimentsId } from "../../funcs/experimentsDeleteApiExperimentsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiExperimentsIdRequest$inboundSchema,
};

export const tool$experimentsDeleteApiExperimentsId: ToolDefinition<
  typeof args
> = {
  name: "experiments-delete-api-experiments-id",
  description: `Delete a Experiment

Deletes a specific Experiment by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsDeleteApiExperimentsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
