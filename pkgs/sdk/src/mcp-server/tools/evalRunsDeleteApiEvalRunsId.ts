/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalRunsDeleteApiEvalRunsId } from "../../funcs/evalRunsDeleteApiEvalRunsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiEvalRunsIdRequest$inboundSchema,
};

export const tool$evalRunsDeleteApiEvalRunsId: ToolDefinition<typeof args> = {
  name: "eval-runs-delete-api-eval-runs-id",
  description: `Delete a Eval Run

Deletes a specific Eval Run by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalRunsDeleteApiEvalRunsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
