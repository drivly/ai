/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalResultsGetApiEvalResults } from "../../funcs/evalResultsGetApiEvalResults.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$evalResultsGetApiEvalResults: ToolDefinition = {
  name: "eval-results-get-api-eval-results",
  description: `List all Eval Results

Returns a list of all Eval Results`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await evalResultsGetApiEvalResults(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
