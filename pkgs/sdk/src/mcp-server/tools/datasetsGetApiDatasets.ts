/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsGetApiDatasets } from "../../funcs/datasetsGetApiDatasets.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$datasetsGetApiDatasets: ToolDefinition = {
  name: "datasets-get-api-datasets",
  description: `List all Datasets

Returns a list of all Datasets`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await datasetsGetApiDatasets(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
