/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { generationBatchesGetApiGenerationBatches } from "../../funcs/generationBatchesGetApiGenerationBatches.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$generationBatchesGetApiGenerationBatches: ToolDefinition = {
  name: "generation-batches-get-api-generation-batches",
  description: `List all Generation Batches

Returns a list of all Generation Batches`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await generationBatchesGetApiGenerationBatches(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
