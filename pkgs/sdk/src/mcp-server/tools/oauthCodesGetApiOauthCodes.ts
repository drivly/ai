/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthCodesGetApiOauthCodes } from "../../funcs/oauthCodesGetApiOauthCodes.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$oauthCodesGetApiOauthCodes: ToolDefinition = {
  name: "oauth-codes-get-api-oauth-codes",
  description: `List all Oauth Codes

Returns a list of all Oauth Codes`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await oauthCodesGetApiOauthCodes(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
