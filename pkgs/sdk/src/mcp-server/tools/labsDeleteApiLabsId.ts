/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labsDeleteApiLabsId } from "../../funcs/labsDeleteApiLabsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiLabsIdRequest$inboundSchema,
};

export const tool$labsDeleteApiLabsId: ToolDefinition<typeof args> = {
  name: "labs-delete-api-labs-id",
  description: `Delete a Lab

Deletes a specific Lab by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labsDeleteApiLabsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
