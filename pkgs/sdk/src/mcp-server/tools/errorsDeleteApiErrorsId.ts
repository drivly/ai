/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { errorsDeleteApiErrorsId } from "../../funcs/errorsDeleteApiErrorsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiErrorsIdRequest$inboundSchema,
};

export const tool$errorsDeleteApiErrorsId: ToolDefinition<typeof args> = {
  name: "errors-delete-api-errors-id",
  description: `Delete a Error

Deletes a specific Error by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await errorsDeleteApiErrorsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
