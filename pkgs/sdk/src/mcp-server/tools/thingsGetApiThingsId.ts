/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { thingsGetApiThingsId } from "../../funcs/thingsGetApiThingsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiThingsIdRequest$inboundSchema,
};

export const tool$thingsGetApiThingsId: ToolDefinition<typeof args> = {
  name: "things-get-api-things-id",
  description: `Get a specific Thing

Returns a specific Thing by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await thingsGetApiThingsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
