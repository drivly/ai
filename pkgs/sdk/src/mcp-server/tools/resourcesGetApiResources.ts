/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesGetApiResources } from "../../funcs/resourcesGetApiResources.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$resourcesGetApiResources: ToolDefinition = {
  name: "resources-get-api-resources",
  description: `List all Resources

Returns a list of all Resources`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await resourcesGetApiResources(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
