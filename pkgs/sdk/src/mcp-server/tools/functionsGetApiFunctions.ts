/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { functionsGetApiFunctions } from "../../funcs/functionsGetApiFunctions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$functionsGetApiFunctions: ToolDefinition = {
  name: "functions-get-api-functions",
  description: `List all Functions

Returns a list of all Functions`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await functionsGetApiFunctions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
