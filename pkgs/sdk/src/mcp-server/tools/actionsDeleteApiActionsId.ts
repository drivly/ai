/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { actionsDeleteApiActionsId } from "../../funcs/actionsDeleteApiActionsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiActionsIdRequest$inboundSchema,
};

export const tool$actionsDeleteApiActionsId: ToolDefinition<typeof args> = {
  name: "actions-delete-api-actions-id",
  description: `Delete a Action

Deletes a specific Action by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await actionsDeleteApiActionsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
