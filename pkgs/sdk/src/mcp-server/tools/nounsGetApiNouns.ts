/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nounsGetApiNouns } from "../../funcs/nounsGetApiNouns.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$nounsGetApiNouns: ToolDefinition = {
  name: "nouns-get-api-nouns",
  description: `List all Nouns

Returns a list of all Nouns`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await nounsGetApiNouns(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
