/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { errorsGetApiErrorsId } from "../../funcs/errorsGetApiErrorsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiErrorsIdRequest$inboundSchema,
};

export const tool$errorsGetApiErrorsId: ToolDefinition<typeof args> = {
  name: "errors-get-api-errors-id",
  description: `Get a specific Error

Returns a specific Error by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await errorsGetApiErrorsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
