/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { generationsGetApiGenerations } from "../../funcs/generationsGetApiGenerations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$generationsGetApiGenerations: ToolDefinition = {
  name: "generations-get-api-generations",
  description: `List all Generations

Returns a list of all Generations`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await generationsGetApiGenerations(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
