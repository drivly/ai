/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalRunsGetApiEvalRuns } from "../../funcs/evalRunsGetApiEvalRuns.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$evalRunsGetApiEvalRuns: ToolDefinition = {
  name: "eval-runs-get-api-eval-runs",
  description: `List all Eval Runs

Returns a list of all Eval Runs`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await evalRunsGetApiEvalRuns(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
