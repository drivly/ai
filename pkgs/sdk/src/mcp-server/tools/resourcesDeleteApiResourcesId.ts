/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesDeleteApiResourcesId } from "../../funcs/resourcesDeleteApiResourcesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiResourcesIdRequest$inboundSchema,
};

export const tool$resourcesDeleteApiResourcesId: ToolDefinition<typeof args> = {
  name: "resources-delete-api-resources-id",
  description: `Delete a Resource

Deletes a specific Resource by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesDeleteApiResourcesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
