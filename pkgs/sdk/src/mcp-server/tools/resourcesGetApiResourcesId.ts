/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesGetApiResourcesId } from "../../funcs/resourcesGetApiResourcesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiResourcesIdRequest$inboundSchema,
};

export const tool$resourcesGetApiResourcesId: ToolDefinition<typeof args> = {
  name: "resources-get-api-resources-id",
  description: `Get a specific Resource

Returns a specific Resource by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesGetApiResourcesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
