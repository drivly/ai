/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchesGetApiSearches } from "../../funcs/searchesGetApiSearches.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$searchesGetApiSearches: ToolDefinition = {
  name: "searches-get-api-searches",
  description: `List all Searches

Returns a list of all Searches`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await searchesGetApiSearches(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
