/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kpIsGetApiKpisId } from "../../funcs/kpIsGetApiKpisId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiKpisIdRequest$inboundSchema,
};

export const tool$kpIsGetApiKpisId: ToolDefinition<typeof args> = {
  name: "KP-is-get-api-kpis-id",
  description: `Get a specific KPI

Returns a specific KPI by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kpIsGetApiKpisId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
