/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchesDeleteApiSearchesId } from "../../funcs/searchesDeleteApiSearchesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiSearchesIdRequest$inboundSchema,
};

export const tool$searchesDeleteApiSearchesId: ToolDefinition<typeof args> = {
  name: "searches-delete-api-searches-id",
  description: `Delete a Search

Deletes a specific Search by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchesDeleteApiSearchesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
