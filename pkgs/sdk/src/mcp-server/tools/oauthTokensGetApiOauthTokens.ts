/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthTokensGetApiOauthTokens } from "../../funcs/oauthTokensGetApiOauthTokens.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$oauthTokensGetApiOauthTokens: ToolDefinition = {
  name: "oauth-tokens-get-api-oauth-tokens",
  description: `List all Oauth Tokens

Returns a list of all Oauth Tokens`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await oauthTokensGetApiOauthTokens(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
