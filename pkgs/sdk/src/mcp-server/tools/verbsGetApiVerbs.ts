/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verbsGetApiVerbs } from "../../funcs/verbsGetApiVerbs.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$verbsGetApiVerbs: ToolDefinition = {
  name: "verbs-get-api-verbs",
  description: `List all Verbs

Returns a list of all Verbs`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await verbsGetApiVerbs(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
