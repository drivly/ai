/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deploymentsGetApiDeployments } from "../../funcs/deploymentsGetApiDeployments.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$deploymentsGetApiDeployments: ToolDefinition = {
  name: "deployments-get-api-deployments",
  description: `List all Deployments

Returns a list of all Deployments`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await deploymentsGetApiDeployments(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
