{
  "name": "ai-workflows",
  "version": "0.1.0",
  "type": "module",
  "description": "AI workflows for orchestrating AI functions",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "ai-workflows": "./bin/cli.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "mkdir -p bin && echo '#!/usr/bin/env node\\nconsole.log(\\\"Stub: Run build first\\\");' > bin/cli.js && chmod +x bin/cli.js",
    "clean": "rimraf dist",
    "lint": "eslint .",
    "test": "echo \"No tests\" && exit 0"
  },
  "dependencies": {
    "ai-functions": "0.3.0",
    "workflows.do": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "tsconfig": "0.0.0",
    "eslint-config": "0.1.0"
  }
}
