#!/bin/sh

# Temporarily disabled as requested for stability
# pnpm lint
# pnpm format
# pnpm generate:types
# Temporarily disabled typecheck as requested
# pnpm typecheck || echo "Typecheck failed but continuing with commit"
echo "All pre-commit hooks temporarily disabled as requested for stability"
