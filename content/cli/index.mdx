---
asIndexPage: true
---

# CLI

The CLI can either be used via `npx` or installed globally via `npm i -g apis.do`.

```bash
npm i -g cli.do
```

## Usage

```bash
apis.do <command>
```

```bash
npx apis.do <command>
```