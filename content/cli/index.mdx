---
asIndexPage: true
---

# CLI

The CLI can either be used via `npx` or installed globally via `npm i -g apis.do`.

```bash
npx apis.do
```

```bash
npm i -g apis.do
```

## Usage

```bash
apis.do <command>
```
