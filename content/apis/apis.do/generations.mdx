---
title: Generations
sidebarTitle: Generations
group: Observability
---

import { TSDoc } from 'nextra/components'

# Generations API

API for managing Generations resources.

## Fields

<TSDoc>
```typescript
interface GenerationsType {
  action?: string | { id: string }
  settings?: string | { id: string }
  request?: object
  response?: object
  error?: object
  status?: string
  duration?: number
  processingMode?: string
  batch?: string | { id: string }
}
```
</TSDoc>


## API Endpoints

### GET /api/generations
Retrieves a list of Generations with pagination.

#### Query Parameters
- `limit`: Maximum number of items to return (default: 10)
- `page`: Page number for pagination (default: 1)
- `sort`: Field to sort by (default: createdAt)
- `where`: JSON query for filtering results

### GET /api/generations/:id
Retrieves a single Generations by ID.

### POST /api/generations
Creates a new Generations.

### PATCH /api/generations/:id
Updates an existing Generations.

### DELETE /api/generations/:id
Deletes a Generations.

