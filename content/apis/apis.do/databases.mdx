---
title: Databases
sidebarTitle: Databases
group: Data
---

import { TSDoc } from 'nextra/components'

# Databases API

API for managing Databases resources.

## Fields

<TSDoc>
```typescript
interface DatabasesType {
  name: string
  domain: string
  type: string
  schemaEnforcement: string
  databaseType?: string
  regions?: string
  nouns?: string | { id: string }
}
```
</TSDoc>


## API Endpoints

### GET /api/databases
Retrieves a list of Databases with pagination.

#### Query Parameters
- `limit`: Maximum number of items to return (default: 10)
- `page`: Page number for pagination (default: 1)
- `sort`: Field to sort by (default: createdAt)
- `where`: JSON query for filtering results

### GET /api/databases/:id
Retrieves a single Databases by ID.

### POST /api/databases
Creates a new Databases.

### PATCH /api/databases/:id
Updates an existing Databases.

### DELETE /api/databases/:id
Deletes a Databases.

