---
title: Oauth-codes
sidebarTitle: Oauth-codes
group: Authentication
---

# Oauth-codes API

API for managing Oauth-codes resources.

## Fields

### code

- **Type**: `text`
- **Required**: Yes

### provider

- **Type**: `text`
- **Required**: Yes

### redirectUri

- **Type**: `text`
- **Required**: Yes

### userId

- **Type**: `relationship`
- **Required**: Yes

### expiresAt

- **Type**: `date`
- **Required**: Yes

### used

- **Type**: `checkbox`
- **Required**: No
- **Default**: `false`

## API Endpoints

### GET /api/oauth-codes

Retrieves a list of Oauth-codes with pagination.

#### Query Parameters

- `limit`: Maximum number of items to return (default: 10)
- `page`: Page number for pagination (default: 1)
- `sort`: Field to sort by (default: createdAt)
- `where`: JSON query for filtering results

### GET /api/oauth-codes/:id

Retrieves a single Oauth-codes by ID.

### POST /api/oauth-codes

Creates a new Oauth-codes.

### PATCH /api/oauth-codes/:id

Updates an existing Oauth-codes.

### DELETE /api/oauth-codes/:id

Deletes a Oauth-codes.
