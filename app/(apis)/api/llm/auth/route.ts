// Route to test authentication, returns a 401 if not authenticated.
// Otherwise returns a 200 with the user's email, and Composio tools they have access to.

export async function GET(req: Request) {}
