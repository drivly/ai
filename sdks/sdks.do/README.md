# SDKs.do

SDK for managing and accessing all Drivly SDKs.

## Installation

```bash
npm install sdks.do
```

## Usage

```typescript
import { getSDKs } from 'sdks.do'

const sdks = await getSDKs()
console.log(sdks)
```
